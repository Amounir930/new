import * as React from 'react';
import { Button } from '@apex/ui';

export const BuilderLayout: React.FC = () => {
  return (
    <div className="p-10 text-center">
      <h1 className="text-2xl font-bold">Builder Layout (Simplified)</h1>
      <Button>Click Me</Button>
    </div>
  );
};
