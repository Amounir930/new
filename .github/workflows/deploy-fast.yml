name: "🚀 Fast Deploy (Build in Cloud)"

on:
  push:
    branches: [New]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: amounir930/adel/api

jobs:
  build-and-push:
    name: "🏗️ Build & Push Docker Image"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,prefix={{date 'YYYYMMDD'}}-

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-ssh:
    name: "🚀 Deploy to Server (SSH)"
    runs-on: ubuntu-latest
    needs: build-and-push
    
    steps:
      - name: SSH Remote Commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Starting Fast Deployment..."
            cd /opt/apex-v2
            
            echo "📥 Pulling latest image..."
            sudo docker pull ghcr.io/amounir930/adel/api:latest
            
            echo "🔄 Recreating API container..."
            sudo docker compose --env-file .env -f ops/docker-compose.prod.yml up -d --force-recreate api
            
            echo "🧹 Cleaning up old images..."
            sudo docker image prune -f
            
            echo "✅ Deployment Complete!"
